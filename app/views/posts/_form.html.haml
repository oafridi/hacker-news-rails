= form_for @post do |f| 
  - if @post.errors.any?
    - @post.errors.full_messages.each do |msg|
      %ul
        %li= msg
        
  = f.label :title
  = f.text_field :title
  = f.label :url
  = f.text_field :url  
  = f.submit "Submit"
